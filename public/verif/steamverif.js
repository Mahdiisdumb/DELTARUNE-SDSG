const steam_verify_config = {
  endpoint: window.location.origin,
  keys_endpoint: `${window.location.origin}/keys`,
  appid: 1671210,
  game_name: "DELTARUNE",
};

window.steam_verify_config = steam_verify_config;
